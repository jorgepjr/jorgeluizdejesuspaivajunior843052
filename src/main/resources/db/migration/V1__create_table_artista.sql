CREATE TABLE artista (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome VARCHAR(150) NOT NULL,
    tipo VARCHAR(50) NOT NULL,
    CONSTRAINT ck_artista_tipo CHECK (tipo IN ('BANDA', 'SOLO'))
);